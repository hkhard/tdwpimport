# PHPUnit
/tests/bin/phpunit.phar
/.phpunit.cache/
/.phpunit.result.cache

# PHPCS + WPCS tooling (installed on demand by run-phpcs.sh)
/tests/tools/

# Composer (optional convenience; harness runs without it)
/vendor/
